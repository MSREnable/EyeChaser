<UserControl
    x:Class="EyeChaser.Controls.BoxTileControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EyeChaser.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Border BorderBrush="LightGray" BorderThickness="2" Background="{x:Bind TileColor, Mode=OneWay}">
            <TextBlock Text="{x:Bind Node.Caption, Mode=OneWay}" FontFamily="{x:Bind FontFamily, Mode=OneWay}" FontSize="{x:Bind FontSize, Mode=OneWay}" TextWrapping = "Wrap" HorizontalAlignment = "Center" VerticalAlignment="Center" 
                       PointerReleased="TextBlock_PointerReleased"/>
        </Border>
    </Grid>
</UserControl>
